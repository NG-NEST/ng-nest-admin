import { Routes } from '@angular/router';
import { SchemaDataComponent } from './schema-data.component';

export const SchemaDataRoutes: Routes = [
  {
    path: '',
    component: SchemaDataComponent
  }
];
