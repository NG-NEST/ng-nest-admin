import { Routes } from '@angular/router';
import { CodeGenerateComponent } from './code-generate.component';

export const CodeGenerateRoutes: Routes = [{ path: '', component: CodeGenerateComponent }];
