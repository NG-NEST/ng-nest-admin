model Model {
  id          String   @id @default(uuid())
  name        String
  code        String   @unique
  description String?
  platform    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  prompts     Prompt[]

  @@map("sys-model")
}
